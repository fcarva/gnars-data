{
  "installCommand": "cd web && npm install",
  "buildCommand": "cd web && npm run build:live",
  "outputDirectory": "web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
